# Anthropic API key — get yours at https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-api03-...

# Vercel Blob (question analytics) — injected automatically when the Blob
# store is connected to the project; for local dev run: vercel env pull
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_...

# Password for the /admin question-analytics dashboard
ADMIN_KEY=choose-a-password
